[pytest]
; Repo root must be importable (webapp/, portscanner/) regardless of how
; pytest is launched — CI runs the `pytest` script, which unlike
; `python -m pytest` does not put the cwd on sys.path.
pythonpath = .
testpaths = tests webapp/tests
